// ? Este archivo contiene la clase Unidad que se encarga de almacenar la información de las unidades de medida. A su vez define como deben  las unidades que se usaran en las formulas como constantes con sus símbolos y nombres.

class Unidad {
  final String simbolo;
  final String nombre;

  Unidad(this.simbolo, this.nombre);
}

// ? Esta clase se encarga de almacenar las constantes que se utilizan en la aplicación con sus nombres, símbolos y valores.
class Constante {
  final String nombre;
  final String simbolo;
  final double valor;

  Constante(this.nombre, this.simbolo, this.valor);
}
